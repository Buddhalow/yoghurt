import Control from '.'

export default class ToolbarPanel extends Control {
    constructor(parent, id) {
        super(parent, id)
        this.buttonStyle = 'default'        
    }
    pack() {
        super.pack()
    }

}